import React from 'react';
import Card from '../../components/Card';

const Home = (props: any) => (
  <div>
    <Card pl={4} pr={4} py={3}>
      <h2>Home</h2>
    </Card>
  </div>
);

export default Home;
